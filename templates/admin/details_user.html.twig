{% extends "admin/layoutadmin.html.twig" %}

{% block title %}Détails de l'utilisateur {{user.firstname}} {{user.lastname}}{% endblock %}

{% block body %}
    <h1> Détails de l'utilisateur {{user.firstname}} {{user.lastname}}</h1>
    
    <h2>Liste des produits </h2>
    
    <ul>
        {% for product in user.products %}
            <li>{{ product.name }}</li>
        {% endfor %}
        
    </ul>
        <a href="{{ path('list_user') }}">Retour à la liste des utilisateurs</a>
       {# TODO afficher les information et produit de l'utilisateur #}
{% endblock %}   
