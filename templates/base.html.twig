<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title> Moijv -{% block title %} Dashboard{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <header>
            <nav>
                <ul>
                    <li>
                         <a href="{{ path('list_user') }}" >Liste des utilisateurs</a>
                         <a href="{{ path('list_product') }}" >Liste des produits</a>
                         {% if is_granted('ROLE_USER') %} {# vérifie les permissions de l'utilisateur #}
                         <a href="{{ path('my_products') }}" >Mes produits</a>
                         <a href="{{ path('add_product') }}" >Ajouter un produit</a>
                         {% endif %}
                         
                    </li>
                        
                </ul>
                
            </nav>
                
        </header>
            
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </body>
</html>
